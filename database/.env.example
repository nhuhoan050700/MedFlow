# Copy to .env and paste your Railway PUBLIC database URL.
# Get it from: Railway → Postgres service → Connect → Public URL
# DATABASE_URL=postgresql://postgres:PASSWORD@HOST:PORT/railway

DATABASE_URL=
